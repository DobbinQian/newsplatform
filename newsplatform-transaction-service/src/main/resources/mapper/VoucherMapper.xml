<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.qdbgame.newsplatform.dao.VoucherMapper">

    <resultMap id="BaseResultMap" type="com.qdbgame.newsplatform.entities.Voucher">
        <result column="voucher_id" property="voucherId"/>
        <result column="user_id" property="userId"/>
        <result column="creation_time" property="creationTime"/>
        <result column="state" property="state"/>
    </resultMap>

    <insert id="insert" parameterType="com.qdbgame.newsplatform.entities.Voucher">
        INSERT INTO voucher (user_id,creation_time,state)
        VALUES (#{userId},#{creationTime},#{state})
    </insert>

    <update id="update" parameterType="com.qdbgame.newsplatform.entities.Voucher">
        UPDATE voucher
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
        </set>
        WHERE voucher_id = #{voucherId}
    </update>

    <select id="getListByUserId" resultMap="BaseResultMap">
        SELECT *
        FROM voucher
        WHERE user_id = #{userId}
    </select>

    <select id="getByUserId" resultMap="BaseResultMap">
        SELECT *
        FROM voucher
        WHERE user_id = #{userId}
        LIMIT 1
    </select>
</mapper>